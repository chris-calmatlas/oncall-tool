extends section

block content
  h2 #{schedule.name}

  p #[strong Users:]
  ul
    each val, index in schedule.members
      li
        a(href=val.url) #{val.name}
    else
      p No users assigned
  hr
  p
    a(href=schedule.url+'/delete') Delete schedule
  p
    a(href=schedule.url+'/update') Update schedule     
